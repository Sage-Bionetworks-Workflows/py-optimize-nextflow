[metadata]
name = py-optimize-nextflow
version = 0.1.0
description = Short script to look at nextflow runs and suggest optimizations
author = Data Processing and Engineering
author_email = dpe@sagebase.org

[options]
packages = find:
include_package_data = True
python_requires = >=3.8
install_requires =
    typer>=0.12.3

[options.extras_require]
dev =
    black>=24.4.2
